package dailypractice.jun;

import javax.persistence.criteria.CriteriaBuilder;
import java.util.*;

public class Sets {
    public static void main(String [] args){
        Set<Integer> hashSet = new HashSet<>();
        Set<Integer> linkedHashSet = new LinkedHashSet<>();
      //  Set<Integer> SortedSet = new SortedSet<Integer>() {
      //  }
        //Red balck tree for sorting in acending order
        //Set-->
        SortedSet<Integer> treeSet = new TreeSet<>();

     //   Map<Integer,Integer> sortedMap = new SortedMap<Integer, Integer>() {
      //  }
//Map--> SortedMap(I)-->treeMap
        SortedMap<Integer, Integer> treeMap = new TreeMap<>();

    }
}
