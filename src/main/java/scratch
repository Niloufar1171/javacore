 System.out.println(Arrays.toString(arr));
   char [] chars= str.toCharArray();

   List<List<Integer>> res = new ArrayList<>();
   res.add(Arrays.asList(start,num-1));

   Collection.sort(list, new ComparatorClass())

   String[] nums1 = version1.split("\\.");
   i2 = i < n2 ? Integer.parseInt(nums2[i]) : 0;

   int product =   str.charAt(i) - '0';

    List<String> l;
   Integer.valueOf(l.get(0)));

     Interval[] intervals
     Arrays.sort(intervals,(a,b)->(a.start-b.start));

     // Interview questions ----------------------------------------------------
     1,2,3,5,8,13,21,34,55
     1, 2 const

     List .add (n-2 + n-1 )
     Public static F List<Integer >(int n){
       if(n == 1 ) return  List.add(1)
      If (n == 2 ) return 2 list.add(2)
       Int  max = f(n-1)
          While ( n < = max)
            Return F(n-1) + F(n -2)
     }
     //print everything less than the input arg 35 →

     def printAll( a : Int ) : Int[] =
      a -> a > Inputa(print{Inputa})

     Val num = list(1,3,5,8,13,21,34,55)

     Main {
     printAll(num)
     }
     //-----------Expedia interview question
     finding a bigest island in ta matrix of 0 and 1
     the ansswer was to use a recursive call 



